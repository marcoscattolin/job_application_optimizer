[project]
name = "job-optimizer"
version = "0.1.0"
description = "AI-powered job application optimizer using Scout, Strategy, and Writer agents"
readme = "Read Me.md"
requires-python = ">=3.10"
dependencies = [
    "loguru>=0.7.3",
    "openai",
    "pydantic",
    "requests>=2.31",
    "beautifulsoup4>=4.12",
    "streamlit>=1.35",
]

[project.scripts]
job-optimizer = "code.run_optimizer:main"
